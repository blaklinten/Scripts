#!/bin/sh

[ -z $@ ] && dmenu -p "Give at lease one argument!" && exit

case "$1" in 
	"start")
		_toggl $@
		[ "$?" != 0 ] && MESSAGE="Something went wrong..." || MESSAGE="$2 was started."
		dmenu -p "$MESSAGE"
	;;
	*)
		_toggl $@ | dmenu -l 30
	;;
esac
